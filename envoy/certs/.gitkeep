# This file keeps the envoy/certs/ directory present in a fresh clone so the
# Envoy image build (COPY certs/) does not fail before the real mTLS material
# is generated by scripts/generate-certs.sh. Generated *.pem files are
# git-ignored and must never be committed.
