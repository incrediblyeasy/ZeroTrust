pytest==8.2.2
httpx==0.27.0
# Used by test_privilege_escalation.py to forge a JWT with an attacker key.
PyJWT==2.8.0
cryptography==42.0.8
# Used by tests/test_offline_security.py to import and exercise the gateway /
# protected-service apps directly (no running stack required).
fastapi==0.111.0
python-jose[cryptography]==3.3.0
python-multipart==0.0.9
